// TODO: Complete this component later
import React from 'react'
import Icon from '../../img/GglIcon';

const GoogleBtn = (props) => {
    return (
        <button><Icon active={true} /> {props.text}</button>
    )
}

export default GoogleBtn;
